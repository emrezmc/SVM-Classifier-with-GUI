## Support Vector Machine Classifier And Regressor with GUI

This is a basic interface that allows us to add datasets, select predictors, targets, tuning parameters, and view results that got from the SVM.

